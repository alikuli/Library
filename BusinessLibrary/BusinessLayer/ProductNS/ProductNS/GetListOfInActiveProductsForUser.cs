

using BreadCrumbsLibraryNS.Programs;
using EnumLibrary.EnumNS;
using ModelsClassLibrary.ModelsNS.ProductNS;
using ModelsClassLibrary.ModelsNS.SharedNS;
using ModelsClassLibrary.ViewModels;
using System.Threading.Tasks;
namespace UowLibrary.ProductNS
{
    public partial class ProductBiz
    {
        //public async Task<IndexListVM> GetListOfUnApprovedProductsForUserAsync(
        //    string userId,
        //    string searchFor,
        //    string isandForSearch,
        //    string selectedId,
        //    string returnUrl,
        //    string logoAddress,
        //    BreadCrumbManager breadCrumbManager,
        //    MenuENUM menuEnum = MenuENUM.IndexDefault,
        //    SortOrderENUM sortBy = SortOrderENUM.Item1_Asc,
        //    bool isMenu = false,
        //    string menuPathMainId = "",
        //    bool isUserAdmin = false)
        //{

        //    Product dudProduct = Factory() as Product;
        //    string dudId = "";
        //    string dudProductId = "";

        //    ControllerIndexParams parms = new ControllerIndexParams(dudId, menuPathMainId, searchFor, isandForSearch, selectedId, menuEnum, sortBy, logoAddress, dudProduct, dudProduct, userId, UserName, isUserAdmin, isMenu, breadCrumbManager, ActionNameENUM.Create, null, dudProductId, returnUrl);

        //    ShowUnApproved = true;
        //    IndexListVM indexListVM = await IndexAsync(parms);

        //    return indexListVM;
        //}
    }
}
